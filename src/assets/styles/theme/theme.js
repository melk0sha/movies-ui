import { buttonSizes } from "assets/styles/theme/buttonSizes";
import { colors } from "assets/styles/theme/colors";
import { buttonColors } from "assets/styles/theme/buttonColors";

export const theme = {
  colors,
  buttonSizes,
  buttonColors
};
