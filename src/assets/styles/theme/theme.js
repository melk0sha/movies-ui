import { buttonSizes } from "assets/styles/theme/buttonSizes.styled";
import { colors } from "assets/styles/theme/colors";
import { buttonColors } from "assets/styles/theme/buttonColors.styled";

export const theme = {
  colors,
  buttonSizes,
  buttonColors
};
