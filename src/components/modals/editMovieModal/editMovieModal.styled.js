import styled from "styled-components";
import { ModalSpan } from "components/modals/shared/styles/modals.styled.js";

export const StyledModalSpan = styled(ModalSpan)`
  margin: 0 10px 10px;
  text-transform: uppercase;
`;
